# 04 — src/data/quests.ts

Status: ready-for-agent
Blocked by: 02

型別 + 查名 helper，對照 `src/data/drops.ts` 的寫法。

- `Quest` / `QuestItem` 介面
- `quests` 陣列
- `questItemName(id)` / `questItemInfo(id)` / `questMapInfo(id)`
- icon 路徑 helper（跟 drops.ts 共用 `/icons/item/<id>.png`）
